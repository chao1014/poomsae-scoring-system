# 品勢計分系統 - 大螢幕投影頁面彙整說明

本文件整理並詳細說明目前「品勢計分系統」投影大螢幕（`ProjectionWindow` 與 `PKProjectionWindow`）中的所有投影頁面、版面配置、資料邏輯及播放時機。

---

# Part 1. 一般賽制投影頁面（個人/雙人/團體）

## 1. 系統等待載入頁面 (System Waiting Layout)
這是大螢幕投影視窗剛開啟、尚未載入任何選手時，或者是上一位選手完賽結束、尚未載入下一位選手時所顯示的等待頁面。

* **顯示時機**：
  * 當大螢幕投影視窗剛開啟，且主控台尚未雙擊載入任何選手資料時。
  * 當點擊主畫面中的「結束」按鈕，或者因取消選取（點擊「取消選擇」）使當前比賽回到未鎖定、未準備狀態時。
* **版面與視覺設計**：
  * **左側底部左格**：顯示狀態文字 `"Waiting..."`（亮黃色大字）。
  * **其餘資訊欄位**：選手姓名、代表單位、籤號、組別與品勢名稱均保持空白，只保留空表格大框外觀，呈現等待新選手載入的狀態。
  * **右側區塊**：常駐顯示裁判狀態燈（J1 ~ J7，依設定顯示啟用的數量）。此時可供裁判端進行連線測試，若裁判端已連線，狀態燈將即時反應為螢光綠色。

---

## 2. 選手介紹頁面 (Player Presentation Layout)
這是比賽載入後、尚未有分數時的常駐準備畫面。

* **顯示時機**：
  * 當系統處於準備中、計分中或尚未完成送分展示時（即 `is_showing_score` 為 `False`，且已載入選手）。
* **版面與視覺設計**：
  * **頂部左側**：顯示選手籤號。
  * **頂部右側**：顯示賽事組別資訊（格式為：`大類 - 組別 - 階段`）。當文字過長時，系統會自動在 `1.5 秒` 延遲後啟用**無縫首尾對接的跑馬燈橫向滾動**。
  * **左側中間上層**：顯示選手代表單位（黃褐色漸層背景，白色大字）。
  * **左側中間下層**：顯示選手姓名（深黃褐色漸層背景，白色大字）。
  * **左側底部左格**：顯示目前狀態文字（如 `"Ready"`、`"Scoring..."`、`"Waiting..."`）或計分倒數計時器。
  * **左側底部中右格**：顯示 1R / 2R 品勢的中英文名稱。品勢語言預設每 `5 秒` 會在中英文間自動切換輪播（例如：`太白型` 與 `Taebaek`）。
  * **右側區塊**：常駐顯示系統設定之啟用裁判狀態燈（如 J1 ~ J5）。會依據裁判端連線與送分狀態呈現不同燈號：
    * *未啟用*：暗紫色。
    * *已連線未送分*：亮螢光綠字。
    * *已送分*：黃色底色搭配深綠色字。

---

## 3. 詳細裁判評分表 (Slide 0 - Detailed Judge Scores Table)
打完分數後，大螢幕展示分數時所顯示的第一個播放頁面。

* **顯示時機**：
  * 當點擊「展示分數」或「重新展示」按鈕後，作為第 0 張投影片播放。
* **版面與資料邏輯**：
  * **首列標題**：顯示 `Judge 1` 到 `Judge N`（依據啟用的裁判數量，字體大小已微調為 `RowH * 0.20`）。
  * **首欄項目**：
    * **細分項目模式**（若有收到裁判提交 `p1` 細分項分數）：第一欄顯示 `Accuracy` (正確性)、`Presentation 1`、`Presentation 2`、`Presentation 3`。其中，`Presentation` 相關標籤字體已優化調小至 `RowH * 0.12`，`Accuracy` 調小為 `RowH * 0.18` 以剛好完美放入黃色格框中。
    * **一般模式**：第一欄顯示 `Accuracy` (正確性，字體 `RowH * 0.18`)、`Presentation` (表現性)。
  * **數據區**：顯示各裁判提交的精確分數。
  * **自動去極值高亮**：
    * 當啟用的裁判人數大於 3 人時，系統會自動算出各項目在計算平均分時需要被剔除的最高分與最低分。
    * 該行最高分與最低分的字體顏色會**自動變更為灰色**（`#7f7f7f`），其餘計入平均分計算的成績則維持白色，以便現場觀眾與選手一眼看出哪些成績已被去極值。

---

## 4. 單輪得分結果頁 (Slide 1 - Single Round Score Result)
在詳細裁判評分表播放完筆後切換而來的單輪分數大字顯示頁。

* **顯示時機**：
  * 緊接在詳細裁判評分表（Slide 0）後播放。
* **版面與視覺設計**：
  * **左側紅框**：展示本輪的「正確性 (Accuracy)」平均得分。
  * **左側藍框**：展示本輪的「表現性 (Presentation)」平均得分。
  * **右側大綠字**：展示本輪的「最終得分 (Final Score)」。已自動扣除極值與現場控分。當分數為滿分 10 分時，將自動顯示為小數點後兩位的 `10.00`，其餘分數維持小數點後三位（如 `9.150`），使字元寬度保持在 5 個字元以符合最佳版面美觀。
  * **右側大白字**：展示本輪的「原始總分 (Raw Total)」（不扣除極值與扣分的加總，字型大小 `0.16Mh`）。
  * **間距優化**：紅、藍框的寬度縮窄為 `0.21Lw`，大字中心向右配置在 `0.75Lw`，大字與左方的藍色框之間留有大約 10% 寬度間距，**完全避免滿分 10.000 時的重疊情形且比例極佳**。

---

## 5. 雙輪進度/得分對照頁 (Slide 2 - Double Round Summary & Comparison)
用以並排展示該選手的 1R 與 2R 成績。

* **顯示時機**：
  * **1 輪制比賽 (Round 1)**：在「單輪得分結果頁」播放完畢後切換顯示。
  * **2 輪制比賽 (Round 2)**：
    * *第一輪（R1）剛打完時*：**不顯示**。為了符合實際比賽流程並維持大螢幕停留，系統播到「單輪得分結果頁」即自動停止輪播（配合 Finished 鎖定機制防止定時重新啟動），停留在此頁面直至工作人員點擊「下一品」為止。
    * *第二輪（R2）剛打完時*：會在「2R單輪得分結果頁」播放完畢後切換顯示，將 1R 與 2R 的成績進行並列。
* **版面與資料邏輯**：
  * **左側對照表格**：顯示包含 `1` (第一輪) 與 `2` (第二輪) 的兩欄對照表。表格內展示各自的總分（綠字）、表現性（藍字）、正確性（紅字）。若 R2 尚未進行，則第二欄數據保持深灰空白。
  * **右側大字區**：
    * *僅第一輪完賽時（如 1 輪制比賽）*：右側大字顯示 **1R 的總得分（大綠字）、表現性（大藍字）、正確性（大紅字）**。
    * *雙輪比賽且 R2 完賽時*：右側大字顯示 **雙輪平均後的總得分（大綠字）、平均表現性（大藍字）、平均正確性（大紅字）**。
  * **對齊與比例**：右側大字 x 座標居中於 `0.70Lw`，y 座標分別分配於 `0.17Mh`、`0.43Mh`、`0.68Mh`，垂直間距更為寬鬆自然，視覺上完全等距且避免重疊。此外，滿分分數亦採用 `10.00` 格式化顯示。

---

## 6. 總排行榜頁 (Slide 3 - Final Leaderboard)
賽事完賽後的當前排行榜。

* **顯示時機**：
  * **1 輪制比賽 (Round 1)**：在「雙輪對照頁」播放完畢後切換顯示。
  * **2 輪制比賽 (Round 2)**：在第二輪（R2）打完、播放完「雙輪對照頁」後切換顯示。
* **版面與高亮效果**：
  * **100% 滿版設計**：排行榜表格橫條底色框左右邊界擴展至 `0` ~ `W`，**完全佔滿大螢幕的左右空間**。
  * **排行榜行數**：最多顯示該組別前 8 名完賽選手的「名次」、「姓名」、「代表單位」與「最終分數」。
  * **剛完賽選手高亮閃爍（黃色字）**：
    * 剛完賽的這名選手那一列（`self.flash_row_idx`）的背景框底色與邊框會以 `500ms` 的間隔進行高亮閃爍（亮藍底色與亮黃邊框交替）。
    * 同時，**該選手那一列的所有字體（名次、姓名、單位、分數）皆會顯示為黃色**（10 分顯示為 `10.00`）。
  * **先前完賽選手（白色字）**：
    * 其他已結束比賽的選手，其那一列字體**皆顯示為白色**，且背景框保持恆暗不閃爍。
  * **零分容許**：即使選手最終得分為 `0` 分（被判 0.00），只要正常參賽完賽，也一樣會被排入排行榜中正常顯示，不會被過濾掉。

---

# Part 2. PK 賽制對抗賽投影頁面

PK 賽制為兩位選手（青方與紅方）同時進行對抗的專用版面，採用左右對稱設計，並支援單輪與雙輪賽制。

## 1. 選手介紹與等待頁面 (PK Player Presentation & Waiting Layout)
選手登場準備或等待下一場比賽載入時的常駐畫面。

* **顯示時機**：
  * 比賽載入後且尚未計分展示時（`is_showing_score` 為 `False`）。
* **版面與視覺設計**：
  * **左半部（青方）**：深藍色漸層背景（`#0f2b5a` 到 `#051124`）。上方顯示青方代表單位（藍字），下方顯示青方選手姓名（白字）。
  * **右半部（紅方）**：深紅色漸層背景（`#5a0f18` 到 `#240509`）。上方顯示紅方代表單位（紅字），下方顯示紅方選手姓名（白字）。
  * **中間區塊**：深色背景搭配垂直排列的裁判狀態燈（J1 ~ J7），可供裁判端進行連線與送分狀態即時回饋。
  * **底部區塊**：顯示賽事階段與進行中的品勢名稱（1R、2R 中文簡稱，如：`太極一章`）。

---

## 2. 詳細裁判評分表 (PK Slide 0 - Detailed Judge Scores Table)
打完分數後，展示青紅雙方各裁判評分明細的對稱表格。

* **顯示時機**：
  * 當點擊「展示分數」後作為第 0 張投影片播放。
* **版面與資料邏輯**：
  * **左右對稱表格**：
    * **左側青方表格**：第一列顯示 `J1` 到 `JN`。其餘列依序展示該裁判提交的 `Accuracy` (正確性) 與 `Presentation` (表現性，若為細分模式則展現 P1, P2, P3)。
    * **右側紅方表格**：同樣顯示該裁判對紅方的評分細節，與左側青方表格呈左右對稱排版。
  * **自動去極值灰色高亮**：
    * 當裁判人數大於 3 人時，自動將被剔除的最高分與最低分更變為灰色（`#7f7f7f`），其餘計入平均分計算的成績維持白色。

---

## 3. 單輪得分結果頁 (PK Slide 1 - Single Round Score Result)
第一輪或第二輪剛打完時，以超大字體突出展示青紅雙方選手該輪的成績。

* **顯示時機**：
  * 緊接在 PK 詳細裁判評分表（Slide 0）後播放。
* **版面與資料邏輯**：
  * **左右對稱大字**：
    * **青方成績（左側）**：上方為最終得分大綠字，中間為原始總分大白字，下方為平均正確性與表現性的分開展示框。
    * **紅方成績（右側）**：對稱展現紅方最終得分大綠字、原始總分大白字，與正確性/表現性框。
  * **滿分 10 分顯示優化**：
    * 當分數剛好為滿分 10.0 時（例如最終得分或平均分），會自動轉換格式顯示為 `.2f` 的 `10.00`，而非 `10.000`。
    * 此調整可將所有分數的字元長度維持在一致的 5 個字元（如 `9.150` 與 `10.00`），避免因為多出一位小數造成版面擠壓或重疊。

---

## 4. 雙輪進度/得分對照與勝負宣告頁 (PK Slide 2 - Double Round Summary & Winner Block)
並排展示青紅雙方選手在 1R 與 2R 的 Accuracy, Presentation, Final Score 成績，並展示最終勝負。

* **顯示時機**：
  * **1 輪制比賽**：在第一輪 Slide 1 播放完後切換顯示。
  * **2 輪制比賽**：
    * *第一輪完賽時*：系統播到 Slide 1 即自動鎖定停留，不顯示對照頁。
    * *第二輪完賽時*：於第二輪 Slide 1 播放完後切換顯示。
* **版面與資料邏輯**：
  * **底部對照明細表格**：在下方分別繪製青紅雙方的 2 x 4 表格，清楚展示第一輪 (1) 與第二輪 (2) 的 Accuracy (橘紅字)、Presentation (天藍字) 與 Final Score (亮綠字) 明細。
  * **上方大字區**：
    * *僅 1R 完賽時*：大字顯示 1R 成績，技術分（表現性）字體調整為與最下方的原始總分一樣大，且垂直位置適當下移避免擁擠。
    * *雙輪完賽時*：大字顯示雙輪平均成績，技術分（表現性）字體調整為與最下方的原始總分一樣大，且垂直位置適當下移。
  * **勝負宣告方塊 (Winner Block)**：
    * 覆蓋在中間原裁判燈的位置上，動態計算文字渲染大小（Bounding Box），以最貼合的緊湊間距繪製粗線外框（左右留白 15px，上下留白 5px），避免外框過大。
    * 顯示文字為兩行，例如 `WINNER\nBLUE` (藍方勝，青色框與字)、`WINNER\nRED` (紅方勝，紅色框與字) 或 `DRAW` (平手，白色框與字)。
